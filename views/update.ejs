<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/css/main.css">
  <title>Todo Update</title>
  <link rel="icon" type="image/x-icon" href="favicon.ico">
  <style>
    body {
      font-family: Arial, sans-serif;
      height: 97vh;
      font-family: Arial, sans-serif;
      background: black;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .container {
      background: white;
      padding: 20px;
      border-radius: 40px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      width: 300px;
    }

    .formadd {
      margin-bottom: 16px;
    }

    input[type="text"] {
      padding: 15px;
      font-size: 15px;
      width: 190px;
      height: 15px;
      border-radius: 30px;
      border: 1px solid black;
    }

    .update {
      display: flex;
      gap: 10px;
      align-items: center;
    }

    .updatebutton {
      padding: 12px 15px;
      color: white;
      border: none;
      border-radius: 30px;
      background-color: green;
      cursor: pointer;
    }

    h1 {
      text-align: center;
      color: black;
    }

    .todo {
      color: rgb(231, 43, 36);
    }

    .list {
      color: green;
    }
  </style>
</head>

<body>
  <div class="container">
    <h1><span class="todo">Todo</span> <span class="list">Update</span></h1>
    <form class="formadd" action="/update/<%=index%>" method="POST">
      <div class="update">
        <input type="text" name="task" value="<%=task.task%>" required>
        <button class="updatebutton" type="submit">
          Update
        </button>
    </form>
  </div>
  </div>
</body>

</html>